# Kimi K3 brief — integrated hardening recheck

## Role and immutable context

Resume the existing K7 parent session as the second-frontier reviewer. This is
a bounded, read-only recheck of the clean Git commit currently checked out in:

`/Users/tommasotessarolo/Developer/metis-model-1`

The pinned evidence source remains strictly read-only at:

`/Users/tommasotessarolo/Developer/ares-matioska/metis@a2dde2b191f6b78c2003d74875560da782470968`

Read `AGENTS.md`, `BLACKBOARD.md`, the active delivery board and ledger, then
the current independence/evaluation/oracle implementation, schemas and focused
tests. Do not read credentials, `.env`, keychains, production payloads or live
ARES data. Do not edit either repository, download, train, stage, commit, push
or publish. Your only writable surface is the ignored activity workspace
provided by the orchestra wrapper.

## Required recheck

1. Record the project branch, commit and exact before/after Git status. Stop if
   the project repository is not clean or changes during the review.
2. Record the Metis commit and exact before/after Git status. Stop on any change.
3. Re-run the focused independence/evaluation and oracle tests.
4. Confirm mechanically that 39 frozen tasks sharing one `benchmark_root`
   produce one component and one frozen leakage group.
5. Confirm the promotion evaluator cannot pass while
   `REGISTERED_TARGET_CONTRACT_SHA256` is unset, and that score, semantic,
   critical, verdict and target-contract evidence are bound to one roster.
6. Confirm oracle execution uses the verified snapshot runner, a registered
   logical runtime identity and the pinned Git/tooling hashes, without exposing
   or writing the source checkout.
7. Recompute the current sourceability and execution blockers from K7 only if
   the integrated implementation changes their arithmetic. Do not weaken the
   563-group or semantic-evidence requirements.

## Output

Return a short final report with P0/P1/P2 findings, commands, exact
denominators, both repository invariants and a clear `ACCEPT` or `STOP`.
Coverage: `units in=3 out=3 distinct=3 gaps=0` for repository invariants,
independence binding and oracle isolation.
