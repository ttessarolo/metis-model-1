# L71 — Exact-three public wheel materialization

## Authority and maximum outcome

The user explicitly authorizes network access only for the three public wheel
preimages already pinned in `uv.lock`. L71 may download, same-file measure and
store those three bytes below the fixed Phase-B source root, then use the
canonical installer APIs to materialize the frozen bundle manifest, install
plan, bootstrap descriptor and resolved administrator invocation. It grants no
OS privilege, service mutation, key generation, Node/Metis execution,
model/dataset download, training, promotion, upload, commit or push.

## Frozen repository and ownership

- repository: `/Users/tommasotessarolo/Developer/metis-model-1`;
- branch: `codex/model1-local-99-foundation`;
- baseline: `2d519d90be9eae0c562a10becbe2bc7e9ac4bbb0` plus the inherited L68-L70
  dirty worktree; no existing change may be reset or overwritten;
- wheel writer surface:
  `/private/var/tmp/MetisModel1-w3-phase-b-source/wheels`;
- artifact writer surface: only the four new canonical L70 production artifacts
  below `manifests/`;
- L0 alone owns this brief, `BLACKBOARD.md` and `SESSIONS.md`;
- all other repository, host, credential, `.env`, key, private/live ARES,
  model and dataset paths are excluded.

## Exact network roster

| Name | Exact HTTPS URL | Size | SHA-256 |
|---|---|---:|---|
| `cryptography-47.0.0` | `https://files.pythonhosted.org/packages/a4/98/40dfe932134bdcae4f6ab5927c87488754bf9eb79297d7e0070b78dd58e9/cryptography-47.0.0-cp311-abi3-macosx_10_9_universal2.whl` | `7912214` | `160ad728f128972d362e714054f6ba0067cab7fb350c5202a9ae8ae4ce3ef1a0` |
| `cffi-2.0.0` | `https://files.pythonhosted.org/packages/4a/d2/a6c0296814556c68ee32009d9c2ad4f85f2707cdecfd7727951ec228005d/cffi-2.0.0-cp313-cp313-macosx_11_0_arm64.whl` | `181043` | `45d5e886156860dc35862657e1494b9bae8dfa63bf56796f2fb56e1679fc0bca` |
| `pycparser-3.0` | `https://files.pythonhosted.org/packages/0c/c3/44f3fbbfa403ea2a7c779186dc20772604442dde72947e7d01069cbe98e3/pycparser-3.0-py3-none-any.whl` | `48172` | `b727414169a36b7d524c1c3e31839a521725078d7b2ff038656844266160a992` |

No index resolution, floating version, alternate wheel, sdist, dependency,
redirect to a non-HTTPS or non-`files.pythonhosted.org` endpoint, or fourth URL
is authorized. Existing targets may be accepted only after exact size/hash
remeasure; otherwise stop without replacement.

## Deterministic gate

1. Census target paths and refuse unknown/pre-existing mismatched bytes.
2. Download each URL to a fresh no-clobber temporary leaf inside the wheel
   writer root; hold or reopen the exact published leaf and recompute size and
   SHA-256 before credit.
3. Report `in=3 out=3 distinct=3 gaps=0` and validate all three as readable ZIP
   wheel preimages without extracting them into Git.
4. Build only through `runtime/w3_broker_installer.py` canonical functions.
5. Validate frozen bundle, exact plan/bundle binding, complete descriptor FILE
   projection and resolved admin invocation; schema, surgical installation
   tests, Ruff/format and `git diff --check` must remain green.
6. A frontier read-only lane and L0 independently recompute hashes, counts and
   at least one mutation before `PHASE_B_INSTALLABLE_UNEXECUTED` may be stated.

The materialized bytes remain unexecuted and uninstalled. Privileged host
qualification and the path to W5 training are later, separately authorized
waves.

## L71.1 corrective materializer extension

The exact-three acquisition closed at `in=3 out=3 distinct=3 gaps=0`, with
independent L0 and read-only frontier remeasurement of every size, SHA-256 and
ZIP preimage. It also exposed two production-construction gaps that must be
fixed before the four artifacts can exist:

1. `runtime/w3_broker_installer.py` has validators, planner, descriptor and
   administrator-invocation APIs, but no canonical source-tree or manifest
   materializer; the test `_fixture_bundle()` is monkeypatched synthetic code
   and is not a production builder.
2. The pinned CPython census is internally impossible: its `2059` rows include
   `251` `.pyc`/`__pycache__` files that the same manifest validator rejects.
   L0 independently measured the symlink-free, startup-injection-free closure
   as exactly `1808` regular files, `44064036` bytes and roster SHA-256
   `b632ae57ee6c013e720fc699380923d807cafa6e82df6b1e96ab9163d7193333`;
   eight symlinks and all 251 forbidden bytecode rows receive zero content
   credit.
3. The exact capsule roster is also rejected by its own validator and schema:
   its `1827` rows contain exactly 32 pinned Git-object rows with role
   `git-archive`, 1793 `tooling`, one `loader` and one `runner`, while the
   accepted role enum omits `git-archive`. The same installer/schema writer
   must add only that registered role and retain rejection of every foreign
   role.

L71.1 authorizes one frontier writer to add the canonical materializer and its
focused tests, and to correct the two bounded CPython/capsule contradictions in
the matching installer and schema. Its exclusive tracked surface is
`runtime/w3_phase_b_materializer.py`,
`tests/test_w3_phase_b_materializer.py`,
`runtime/w3_broker_installer.py` and
`schemas/w3-phase-b-install-bundle.schema.json`. Read-only inputs are limited
to the exact local CPython 3.13.3 root, the exact local Node v22.22.3 binary,
the pinned Metis Git object/tooling closure, current project runtime/source and
packaging bytes, the two reproducible Stage-0 builds and the three frozen
wheels. The Metis repository is read-only and no Node, Metis, model, dataset or
training process may execute.

External output is limited to new no-clobber children of
`/private/var/tmp/MetisModel1-w3-phase-b-source/{artifacts,install-root,source-census,metadata}`;
tracked output remains limited to the four new canonical manifest leaves. The
builder must deterministically generate every static config/policy/plist,
reproducibly build and measure the three native binaries, safely expand the
three exact wheels, reconstruct the exact `1827/8922291/d72a8a4…` capsule,
project the complete source/install rosters, and pass the existing validators,
schema and mutation gates. Any missing local input or denominator drift stops
without a handcrafted JSON fallback.

The independent pre-write audit also found that the administrator document
bound only the privileged Stage-0 target while the two reproducible binaries
remained under random ephemeral build directories. L0 therefore ratifies one
additional durable external preimage leaf, outside the Stage-0 strict source
tree: `/private/var/tmp/MetisModel1-w3-phase-b-bootstrap/w3-installer-bootstrap`.
It is published as a regular `0555` no-clobber file only after build A and B
are byte-identical at `54232/ddd09fcf…`. The bundle/template and administrator
document must bind this source separately from target
`/private/var/db/MetisModel1/w3-installer-bootstrap`, with identical size/hash
and an explicit trusted `/usr/bin/install` copy plus target remeasurement before
execution. This adds no privilege or execution authority and does not add an
unlisted file to `BOOTSTRAP_SOURCE_ROOT`.

Stage-0 projects every non-install source row as mode `0444`. The three wheel
bytes initially published as `0600` were therefore not copyable under the
closed descriptor contract. L0 finalized only those three exact inodes to
`0444`; pre/post inode, size and SHA-256 remained identical. The materializer
must now require, never silently repair, the exact read-only wheel modes.

## L71 closure evidence

L0 froze `runtime/w3_phase_b_materializer.py` at SHA-256
`579b432162904320eb01315208ee8815d5c600241ce543ed59b3cfde4835ab72`.
The final focused suite closed `67/67`; schema, Ruff, format and diff gates were
green, and the independent bounded review returned `P0=0`, `P1=0`, `P2=0`.
The transaction uses complete staged files plus atomic no-replace rename,
immutable inode ledgers, strict rollback-incomplete reporting and exact
second-run adoption. Its precondition is exclusive-writer ownership of the
random `0700` workspace; hostile same-UID/root mutation inside that already
bound workspace is a nonclaim.

L0 then executed the authorized fixed-root transaction. The physical source
tree closed at `7477` regular files and `372424009` bytes: artifacts `20`,
install root `3817`, source census `3635`, metadata `2`, wheels `3`. Stage-0 and
all four tracked outputs were created once and adopted on the second call with
all nine published inodes unchanged and zero staging residue. The frozen bundle
digest is `sha256:9bacb3463c015e72d85d2283f07db13a63e17a9291c9bc8a4de2eadc6b029ec0`.
This establishes only `PHASE_B_INSTALLABLE_UNEXECUTED`; it grants no privilege,
service activation, key, live Oracle receipt, dataset, training or promotion
credit.
