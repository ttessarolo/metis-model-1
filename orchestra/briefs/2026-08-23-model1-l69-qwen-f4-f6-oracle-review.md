# Qwen 3.8 Max L69 — F-4/F-5/F-6 typed-oracle frontier review

You are the Qwen `qwen3.8-max` frontier master at maximum available effort.
Operate under the injected Orchestra master protocol: decompose, delegate only
enumerated independent units, validate returned work and deposit facts in the
shared activity blackboard as soon as they are known.

## Immutable context

- repository: `/Users/tommasotessarolo/Developer/metis-model-1`;
- branch: `codex/model1-local-99-foundation`;
- baseline HEAD: `2d519d90be9eae0c562a10becbe2bc7e9ac4bbb0`;
- preserve all inherited dirty L66-L68 bytes; project repository is read-only;
- L68 Phase A is accepted only as
  `BROKER_DESIGN_ACCEPTED_PAYLOAD_FREE`; current real W3 execution credit is
  still `0/15`;
- F-1/F-2/F-3 infrastructure must not be weakened or credited by static wiring.

Read first the injected `BLACKBOARD.md`, then the project delivery board and
ledger, `docs/00-charter-and-decisions.md`, `docs/03-evaluation-and-gates.md`,
`docs/06-delivery-roadmap.md`, `docs/13-protected-execution-broker.md`, and the
current W3 oracle/adapter/worker/qualifier schemas, code and focused tests.

## Scope

Produce an adversarial, implementation-ready gap review for F-4/F-5/F-6.
Decompose into no more than three bounded units and validate:

1. the exact current support surface for F-1/F-2/F-3 versus what is missing for
   F-4/F-5/F-6, with callsite/schema/test evidence;
2. the typed semantic specifications and independent oracle results needed for
   each missing family, including negative/mutation cases and which evidence can
   only come from the protected Phase-B runner;
3. the minimum disjoint writer roster and focused denominator required to
   implement the missing families without allowing adapter-authored
   `matched=true`, compiler-clean output, policy-only receipts or synthetic
   receipts to count as semantic/production evidence.

Independently recompute at least one claim per unit. Report
`in=N out=N distinct=N gaps=0`, dependencies, stop rules and exact acceptance
commands. Prefer the smallest compatible extension of current contracts; do not
recommend a parallel execution stack.

## Hard boundaries

- only the injected ignored activity board, journal and artifact report are
  writable;
- no Node/Metis execution, privileged action, live ARES access, credentials or
  `.env`, external upload, payload/model download, training, promotion, commit or
  push;
- do not grant real receipt, oracle, semantic or production credit from static
  code or tests;
- do not modify the accepted Phase-A broker contract.

## Output

Write `qwen-l69-f4-f6-oracle-review.md` in the injected activity artifacts
directory. End with `ACCEPT_PLAN|REWORK`, P0/P1/P2 counts, coverage arithmetic,
exact proposed writable roster and the shortest dependency-ordered gate plan.
