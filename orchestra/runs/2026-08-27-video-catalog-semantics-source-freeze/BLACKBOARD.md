# Video catalog semantics source-freeze blackboard

## Objective

Close the executable P0 source-freeze path and advance P1/P2 only where current
evidence is complete, while keeping every confidential source identity and
payload outside Git, logs, prompts and public receipts.

## Baseline and authority

- FACT — Start baseline is clean `main@421bc30cda08cec4f4ec7d3641ad8a0faa8249c9`,
  aligned with `origin/main`.
- FACT — The preceding private-boundary run is terminal historical evidence and
  remains immutable.
- DECISION — L0 alone owns architecture, semantic interpretation, privacy and
  leakage judgments, integrated gates, commit and publication.
- STOP — No source identity, filename, locator, digest, dimension, excerpt or
  content may enter Git, official documentation, delegated prompts or public
  command output.
- STOP — No credential store, live service, raw tenant payload, canonical
  catalog write, model training or adapter mutation is authorized in this run.
- STOP — Confidential text may not enter a frontier runner until egress denial
  is proved for the exact executable, model peer and network boundary.

## Grammar gate recheck

- DONE — `SEMANTIC_GRAMMAR_SURFACE_FROZEN` is pinned to promoted
  `metis@53fe68949ce4a0a46fd1825c1a071255c500eec6`, aligned with its remote branch.
- DONE — `FULL_CORPUS_GRAMMAR_COMPAT`: `404` documents traversed, no unexpected
  errors and no keyword collisions.
- DONE — `CATALOG_SEMANTICS_ARTIFACT_INVARIANT`: Catalog, Field, ValueItem and
  ListEntry semantic-only variants preserve the compiled artifact set.
- DONE — `REFLECTED_SEMANTICS_PRESERVED`: exact-literal survivors, add/remove,
  inline/value-set transitions, open-domain stop, stale-source protection and
  idempotence are green.
- FACT — L0 independently reran typecheck and all four targeted gate programs
  against the pinned committed tooling surface.
- FACT — After the freeze commit, the upstream team opened the successor
  semantic-retrieval lane in its working checkout. L0 therefore repeated the
  grammar tests from a clean temporary extraction of the pinned commit instead
  of consuming the moving checkout.
- RISK — The successor retrieval delta is not yet a promoted dependency. Any
  future upstream write must use a separate clean worktree and a distinct
  mandate. This run is read-only toward that repository.
- FACT — The successor schema-2 retrieval candidate passes its dedicated
  compatibility, semantic, R8, full-corpus, typecheck and repository tests in
  the upstream working checkout, but it is still uncommitted and its dedicated
  test is not yet part of the default test chain.
- OPEN — `SEMANTIC_CATALOG_READY`: retrieval schema 2 still needs promotion,
  pinning, test-chain adoption and downstream consumer verification.

## Current gate roster

- DONE — `VIDEO_PRIVATE_ARTIFACT_BOUNDARY_VALID`: directory relocation,
  pre-link source swap, post-publish target swap and same-inode mutation are
  blocked and cleaned; `29/29` dedicated adversarial tests and L7 exact reruns
  are green.
- DONE — `VIDEO_SEMANTICS_SOURCES_FROZEN`: the private roster is complete,
  `gaps=0`; stable freeze v2 excludes operational timing/run volatility and
  independently recomputes byte-identically through the repaired boundary.
- DONE — `VIDEO_SOURCE_EXTRACTION_PREFLIGHT_VALID`: the real local parser run
  and its idempotent replay returned only the allowlisted public contract with
  complete roster, supported formats, verified sandbox and `gaps=0`; the
  private envelope was independently re-extracted and matched exactly.
- OPEN — `EDITORIAL_SOURCE_ONTOLOGY_CENSUS_VALID`: pure validation exists, but
  no confidential ontology has been produced or validated.
- OPEN — Unit-level census disposition: source-ref coverage alone does not
  prove that every extracted page/document unit was inspected. Ontology
  promotion requires an exact private roster assigning every unit a terminal
  disposition (`concepts`, `no_concept` or reasoned exclusion) with
  `in=out=distinct` and `gaps=0`.
- OPEN — `VIDEO_LOCAL_SNAPSHOT_CENSUS_VALID`: the authorized versioned snapshot
  is structurally partial and cannot prove the complete catalog.
- OPEN — `VIDEO_PRELIMINARY_CROSSWALK_VALID`.
- OPEN — `FRONTIER_EGRESS_DENY_VALID` for the exact confidential drafting
  runner; synthetic boundary evidence is not promoted to this claim.
- DECISION — P1 editorial extraction must use the pinned local base model with
  the Model 1 adapter disabled. The adapter remains reserved for the later
  validated-ontology-to-`.metis` path because it is not an ontology extractor.
- STOP — The existing evaluation worker and synthetic egress test are not an
  authority for private ontology generation. A dedicated path with exact-peer
  offline canaries, allowlisted environment, private-only output and a
  self-hashed receipt is required before any confidential text reaches a model.

## Evidence log

- FACT — Three bounded Orchestra lanes independently rechecked upstream grammar,
  private-I/O gaps and the local versioned snapshot. Delegates performed no
  writes, publication, credential access or live calls.
- FACT — Current Model 1 semantic-boundary tests pass, and the real boundary
  bootstrap returned `VALID` with no sensitive material accessed.
- FACT — Real reserved-source acquisition returned only the allowlisted public
  result `VALID`, `private_roster_complete=true`, `gaps=0`; names, hashes,
  counts, locators and content remained confined to the ignored private store.
- FACT — Real source extraction and its second replay returned `VALID`,
  `private_roster_complete=true`, `format_supported=true`,
  `sandbox_verified=true`, `gaps=0`; no private field was emitted.
- FACT — L0 independently reloaded and revalidated the private envelope,
  receipt roster and source freeze; recomputation matched exactly and Git
  status exposed no artifact.
- FACT — Final `make check` reaches `passes=80 errors=2 files=458`; both errors remain
  the historical T30-v3 freeze-linkage and pinned-Node replay blockers. They do
  not substitute for or close a video-semantics gate.
- DONE — L7 reran the exact integrated public/synthetic roster `180/180`:
  semantic contracts, artifact boundary, private I/O, acquisition, extraction,
  tooling, private runner, CLI and the foundation fail-closed regression. Ruff
  formatting/lint and diff checks are green.
- RISK — The versioned tenant snapshot is a partial demonstration surface, not
  a complete catalog census, and has no current compiled artifact receipt.
- DECISION — P0 secure persistence and CLI closure proceed now with synthetic
  tests. Confidential acquisition may run only through the local path-silent
  command after L0 accepts the implementation.
- FACT — The no-egress design review found the storage and ontology validator
  reusable but the model worker absent. This leaves P1 production and
  `FRONTIER_EGRESS_DENY_VALID` open without weakening the completed P0 freeze.
- RISK — A proposed ontology-worker implementation lane reported that an
  over-broad local search crossed the ignored artifact tree and surfaced only
  private metadata in its own local tool output. The lane was interrupted,
  produced no accepted implementation, opened no source payload and made no
  external transmission. Its output is rejected and the ontology-worker gate
  remains open.
- FACT — Independent synthetic adversarial tests had reproduced a successful
  write after parent-directory relocation and a failed hard-link publication
  that left an accepted poison target. Promotion was suspended while those
  failures remained open.
- FIX — The private writer now retains the full directory-FD chain, revalidates
  ancestry and target state around publication/postflight, holds the source FD
  through the hard-link claim and removes the claimed target on any failure.
- DONE — L7 independently reran all four write-race repros and accepted their
  closure. The stable-freeze repro also passes across operationally different
  receipts for identical evidence.
- FIX — The extraction contract now binds root-owned parser provenance,
  page/document unit rosters, exact integer types, deterministic unit counts
  and deny-canary silence; rehashed bool substitutions and denied-read output
  are rejected fail-closed.
- FIX — Editorial concepts now use host-owned deterministic IDs, kind-specific
  cardinality semantics, reciprocal parent/child edges, symmetric exclusions
  and acyclic hierarchy validation.
- FIX — Ontology validation now requires a self-hashed private disposition
  roster bound to both the extraction envelope and ontology bytes. Every
  source/unit pair must terminate exactly once as `concepts`, `no_concept` or
  reasoned `excluded`; no one-unit legacy bypass remains.
- FIX — Acquisition now ignores harmless content-metadata churn only for
  ancestors outside the authorized source root. Directory identity/security,
  parent-entry binding and full source-root/subtree drift checks remain strict;
  relocation/replacement adversaries are still blocked.
- FIX — The authorized source root, every internal directory and every source
  file must now be owner-controlled and not group/world-writable. Ordinary
  read-only sharing modes remain valid; mutable external ancestors such as a
  sticky temporary parent are not mistaken for source evidence.
- FIX — Private-store Git authority is now the verified root-owned system
  executable with root-owned non-writable ancestry, pre/post identity checks,
  a bounded environment and optional locks/config redirections disabled. Fake
  PATH, `GIT_DIR` and `GIT_INDEX_FILE` cannot redirect the boundary probes.
- DONE — L7 independently accepted the permission and Git-authority adversarial
  roster: boundary/private-I/O/acquisition `76/76`, Ruff and format green.
- DONE — Final L7 verdict accepts `VIDEO_PRIVATE_ARTIFACT_BOUNDARY_VALID`,
  `VIDEO_SEMANTICS_SOURCES_FROZEN` and
  `VIDEO_SOURCE_EXTRACTION_PREFLIGHT_VALID` with no residual P0/P1 finding.
- DONE — The suspension is lifted for P0 and the mechanical P1 source-text
  preflight only. Ontology drafting, frontier egress, editorial review and all
  downstream promotion gates remain open.

## Active exit criteria

1. private-store I/O is contained, no-follow, single-link, owner-bound, `0600`,
   atomic, no-overwrite and Git-status invariant;
2. `freeze-sources` validates an exact private manifest/receipt roster and
   persists a deterministic local-confidential freeze;
3. `validate-ontology` reads only bounded private relative paths and prints only
   the public-safe result contract;
4. `extract-sources` uses the real sandboxed parser path, persists only to the
   ignored private store and passes an exact independent replay;
5. synthetic tests, privacy scans, Ruff, formatting and diff checks are green;
6. L0 inspects the integrated diff and independently reruns the real commands;
7. no confidential material appears in Git or any official evidence surface.

## Status

`VIDEO_SOURCE_EXTRACTION_PREFLIGHT_VALID_ONTOLOGY_OPEN`
