# Metis Brain hard headless qualification

Status: **ACTIVE — HEADLESS FOUNDATION GREEN; LIVE QUALIFICATION OPEN**

## Mandate

Qualify the ten complex play-prod endpoint families outside VS Code through the
real local Metis Brain HTTP contract. Exercise both surfaces already frozen in
the inspectable corpus:

1. ten existing-endpoint edit requests;
2. ten create-from-zero journeys, each with four logical operator turns;
3. universal typed clarification answers and proposal lineage;
4. schema-2 retrieval, Model 1/Flash where admitted, full-snapshot compiler and
   semantic/output oracles;
5. redacted per-turn evidence and exact tenant/model guards.

This is a qualification wave, not an Apply or product-mutation wave. A truthful
failure, typed clarification, unsupported result or fail-closed result is an
accepted measurement outcome; it is never relabelled as a successful Draft.

## Preflight

- Model 1 repository: `/Users/tommasotessarolo/Developer/metis-model-1`;
- baseline: `ac8c088d4c17e0bdcba7217d6c7e131befc5bd00`, clean `main`, aligned
  `0/0`;
- frozen prompt corpus:
  `examples/metis-brain-hard-prompts.play-prod-v1.json`;
- corpus roster: existing edits `10/10`, create journeys `10/10`, logical
  create turns `40/40`; total logical operator messages `50`;
- source tenant: `/Users/tommasotessarolo/Developer/metis-tenant-play-prod`,
  clean `main` at `5f56bdfe27e3fb00b735db630a4eb5cdf5ab12c3`, tree
  `03abf1a30603ff6cb59d55c32c3395cef868a218`;
- current Metis authority: `/Users/tommasotessarolo/Developer/ares-matioska/metis`,
  commit `3fde0820c04244b011a2f7a9604c425891424b34`, current Brain toolchain pin
  `metis-brain-toolchain/2026-09-04-v2`;
- local base checkpoint, step-50 adapter, qualified Node, Model 1 Python runtime
  and Flash payload were observed present by the read-only authority lane;
- the user's 2026-09-04 instruction authorizes this one local model execution
  wave. It does not rewrite the historical corpus safety declaration.

## Authorities and exclusions

- Local MLX only. No Ollama, remote fallback, external network or model
  download.
- No VPN, ARES/OpenSearch, credentials, `.env`, Keychain, AWS or S3.
- Canonical tenant roots are read-only. Brain may snapshot, retrieve, generate
  and compile; it may not call Apply or write a tenant.
- The qualification client receives no `chat.apply-preflight` capability.
- Runtime outputs live only below ignored
  `artifacts/metis-brain-hard-qualification/`; no prompt, generated source,
  bootstrap token or session token enters Git or public logs.
- No change to model, adapter, dataset, grammar, standard library, Metis,
  Metis Fast or VSIX is authorized by this wave.

## Required source documents

- `docs/00-charter-and-decisions.md`;
- `docs/06-delivery-roadmap.md`;
- `docs/23-metis-brain-local-runbook.md`;
- `docs/26-metis-brain-interactive-session-wave.md`;
- `docs/27-metis-brain-local-latency.md`;
- `docs/28-metis-brain-flash-intent-compiler.md`;
- `examples/metis-brain-hard-prompts.play-prod-v1.json`;
- `src/metis_model1/brain_latency_live.py` and the current HTTP/turn contracts.

## Exit gates

1. `CORPUS_AUTHORITY_GREEN`: exact committed corpus bytes, source snapshot and
   10 + 10 + 40 rosters are pinned and revalidated.
2. `HEADLESS_WIRE_GREEN`: a tested local runner uses the real numeric-loopback
   HTTP session/turn/answer/read/close contract, not VS Code or a fake model.
3. `NO_APPLY_GREEN`: the qualification client has no Apply capability and the
   runner contains/calls no Apply surface.
4. `TENANT_INVARIANCE_GREEN`: HEAD, tree, status, snapshot roster and each
   target hash are unchanged before/after every case and at suite closure.
5. `EDIT_10_MEASURED`: all ten existing edit requests have one terminal,
   truthfully classified result and redacted receipt.
6. `CREATE_10X4_MEASURED`: all ten four-turn logical journeys are attempted in
   order; every admitted continuation uses server-owned proposal basis and
   every typed question uses `/answer`. A blocked predecessor explicitly
   blocks, rather than fabricates, its dependent turns.
7. `ORACLES_REPORTED`: every Draft is separately checked for compiler status,
   exact grounding, target/output invariants and proposal lineage; compile
   clean alone never becomes semantic success.
8. Focused tests, Ruff/format, diff review, authoritative repository gate and
   clean aligned `main` are green before closure.

## Evidence wire

- FACT — Read-only Orchestra census found the historical corpus structurally
  complete: edits `in=10 out=10 distinct=10 gaps=0`; create journeys
  `in=10 out=10 distinct=10 gaps=0`; logical create turns
  `in=40 out=40 gaps=0`.
- FACT — Every complex edit is outside the currently admitted one-IncludeClause
  lossless subset. A result may use the full Model 1 path only if existing
  fallback/output preservation authority is complete; otherwise Brain must
  fail closed.
- FACT — The tracked demo config grants only `play-demo`; a separate minimal
  qualification config is required for the pinned `play-prod` source tenant.
- RISK — The pinned play-prod tenant currently exposes no `means` annotations
  in its catalog sources. The schema-2 retrieval preflight must establish
  whether the current reviewed-only grounding contract can execute these cases;
  no semantic hybrid fixture may be silently substituted.
- OPEN — Implement the bounded headless runner, config and synthetic protocol
  tests; then run the real 10-edit + 10-journey matrix once and publish the
  redacted receipt digest and scorecard.
- FACT — Direct pinned catalog census on the source tenant reports catalog
  `play-prod-v2.video` plus 111/111 fields in state `unannotated`, 14 finite
  domains, 0 open domains and 97 domain-none fields. This is a measured
  semantic-authority risk for the live run, not permission to substitute the
  annotated play-demo catalog or an unpinned hybrid.
- FIX — The execution plan binds all ten source hashes and exact edit
  replacements. It records one historical-corpus erratum without rewriting
  v1: the `Le serie di Mediaset Infinity` occurrence is at pinned source line
  449, not prose line 450.
- DONE — Deterministic runner foundation `in=30 out=30 distinct=30 gaps=0`:
  focused qualification/corpus/answer/config tests pass; targeted Ruff and
  format checks are green. The minimal qualification client exposes exactly
  five read/session/turn capabilities and no Apply or direct compile
  capability.
- OPEN — Independent L904 safety review and L905 state-machine review precede
  the clean-main live MLX execution.
- FACT — The qualification assessment covers `10` existing-endpoint edit
  journeys plus `10` create journeys: `30` generated Draft turns were assessed
  against `50` logical operator messages (`10` edits + `40` create turns).
- FACT — Exact execution pins are sealed in the tracked qualification
  constants/config: corpus
  `sha256:6022ea8104a0b01deacd81bf4f46bd78d72154a308278081ecddfcc6f1bc119c`,
  plan
  `sha256:f3aa5efbeed73b14f9805ee42b6906faa3bda2fdd8bab498ddc52323af5ccf5c`,
  config
  `sha256:b1e4256a4f1bdd11b3485b7554b6e9c1c8666c034947a99da48c1267f85e2842`,
  tenant `play-prod-v2` at the pinned source root, HEAD
  `5f56bdfe27e3fb00b735db630a4eb5cdf5ab12c3` / tree
  `03abf1a30603ff6cb59d55c32c3395cef868a218`, Metis
  `3fde0820c04244b011a2f7a9604c425891424b34` / tree
  `432bd3babd9f4c2dfe6349288b12eba917d4fe73`, Model 1 revision
  `3e6447f082e89cc7f0bc6e5441afd38dfce760ff` with adapter
  `sha256:5e65a0b48531ce9e2a9751c201f570f8793da87bd2a2a9446f461dbe0589dcfb`,
  Flash revision `475b9088d29754a3379866cf5aeb6b41acd313c2` with schema
  `sha256:972eb339d8f0f22f4d5dd43aa9f4f74ae49e2a6e2b3b7ff536a60444edd864fa`,
  and runner `sha256:5497794fe0bbedf84639e40a2e7a8a9143feb661080510fc3b115642a690f432`.
- FACT — The private compile-structure oracle compiled all `10/10` pinned
  source endpoints. The P0 false-green gate is fixed by requiring structural IR
  facts and final normalized-IR equivalence; compile-clean alone remains
  insufficient for semantic success.
- OPEN — The real live 10-edit + 10-create headless MLX run has not yet been
  executed; no live Draft/pass claim is made.
- STOP — Live qualification remains fail-closed until its redacted receipt is
  produced and independently checked against the pinned guards and oracle
  gates.
- FIX — L908 adversarial review found two intermediate-oracle blind spots even
  though final normalized-IR equivalence was already exact. The sealed plan now
  requires zero rendered output flows before `similar_intrat_abtest` turn 4 and
  binds personalized similarity ordering to
  `user.video_historical_fingerprint`, not merely `similarity:desc`. The plan
  now contains `139` closed structural checks.
- DONE — Independent pre-live reviews: L904 security/fail-closed GREEN with no
  P0/P1; L905 CLI, denominator and state-machine GREEN with no P0/P1; L908 IR
  semantics GREEN after the two surgical oracle fixes. No reviewer started a
  model, used the network or mutated a tenant.
- FACT — The first authoritative `make check` completed `2981 passed, 2
  skipped` but correctly returned non-green at the post-suite authority guard.
  A read-only metadata watcher identified the sole drift as
  `.claude/worktrees/git-ambienti-residui/tooling/src/extension/experiment-git.ts`,
  inside an administrative nested worktree explicitly ignored by the Metis
  repository. Metis HEAD/tree/status and both pinned Node binaries remained
  unchanged.
- FIX — The source-worktree fingerprint now excludes exactly the
  `.claude/worktrees` prefix, as it already excludes `.git` and the separately
  pinned `tooling/node_modules`. A regression test proves mutations below that
  administrative prefix are ignored while an ordinary `.claude/settings.json`
  mutation is still detected. The full gate must be rerun; the first pass is
  not accepted as GREEN.
- DONE — L909 independent security review of the bounded harness exclusion is
  GREEN with P0=0, P1=0, P2=0. Focused verification reports 40 tests passing
  across the harness and hard-qualification surfaces; Ruff and diff checks are
  green. A direct source fingerprint completes in 0.56 seconds after excluding
  the nested administrative worktrees.
- FACT — A second full gate reached `2982 passed, 2 skipped` but the post-suite
  authority guard again returned non-green. This time the canonical Metis
  checkout had cleanly advanced by 24 commits to
  `42eecd16d72f818f53964989913f4ef1e24a0191` / tree
  `659d8f5492867417cff6c684e589431283578dc2`; the qualification pin
  `3fde0820c04244b011a2f7a9604c425891424b34` remains its ancestor and the
  isolated suite continued executing the exact pinned archive. The failed gate
  is not accepted as GREEN.
- FIX — The source checkout is now required to be Git-clean before and after
  the suite, while a concurrent clean HEAD advance is allowed. Pinned
  revision/tree and external node_modules remain hash-bound before and after;
  the executed temporary repository still binds detached HEAD/tree, clean
  status, alternate object authority and copied node_modules before and after.
  Focused tests prove a clean concurrent commit leaves the isolated revision
  unchanged and an uncommitted tracked mutation still fails closed.
- DONE — L909 second independent review of the concurrency-aware authority
  guard is GREEN with P0=0, P1=0, P2=0. The reviewer confirmed that clean HEAD
  movement cannot alter the detached pinned archive, while source dirt,
  pinned-runtime drift, alternate drift or isolated HEAD/tree/status drift
  still fail closed.
- DONE — The coordinator reran the authoritative repository gate after the
  final guard change: `make check` completed `2982 passed, 2 skipped, 0 failed`
  in `1896.24s` (`31m36s`); foundation, closure, asset, dataset and evaluation
  checks passed, and Ruff plus format checks were green. This is the accepted
  full-gate run for the headless foundation.
- FACT — At accepted-gate closure the pinned tenant remained clean at
  `5f56bdfe27e3fb00b735db630a4eb5cdf5ab12c3` / tree
  `03abf1a30603ff6cb59d55c32c3395cef868a218`. The canonical Metis source
  checkout had cleanly advanced to
  `132562abd419f92c89501f4b20bb81164d05f990` / tree
  `5b969fccbcd379e3b9738a452b58814b15386546`; the executed toolchain pin
  `3fde0820c04244b011a2f7a9604c425891424b34` remains its ancestor and the
  isolated authority stayed bound to that pin.
- OPEN — Commit and push the verified headless foundation, then execute the
  one authorized live local-MLX qualification and inspect its receipt. No live
  Draft, semantic-pass or accuracy claim exists before that receipt.
