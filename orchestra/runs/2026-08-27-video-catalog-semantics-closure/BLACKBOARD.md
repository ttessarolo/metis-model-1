# Video catalog semantic-grounding closure blackboard

## Objective

Close every automatable P1-P14 deliverable for the `@video` vertical, prepare
the local-confidential path without disclosure, and reduce the wave to no more
than exact external or human gates which cannot truthfully be substituted by
repository code.

## Baseline, authority and boundaries

- FACT — Model 1 starts from clean `main@c6e599b1dfdeb9a8dea92f334e97bdc3d84be63d`,
  aligned with `origin/main`.
- FACT — P0 source freeze and P1 mechanical extraction preflight were accepted
  on the predecessor board. That board remains immutable evidence.
- DECISION — L0 alone owns architecture, confidential execution, semantic and
  leakage judgment, integrated gates, commit and push.
- STOP — Delegates may not access reserved source content, ignored private
  artifacts, credentials, `.env`, Keychain, live tenant data or external write
  surfaces.
- STOP — Upstream Metis and tenant repositories are read-only. This run consumes
  committed Git objects and local versioned source only.
- STOP — No model download, training, adapter mutation, canonical tenant patch,
  live write or remote frontier egress is authorized.

## Upstream delivery

- DONE — Promoted Metis `main` and `origin/main` are both
  `0b41a25d4d5eeac88975e43e18e4bc3123d51667`.
- DONE — Grammar, R8, structured sync and full-corpus ancestors are committed;
  semantic retrieval schema 2 landed at `a84d3db0` and its test is in the
  default test chain.
- DONE — L15 clean-archive audit: typecheck and all seven targeted gate programs
  exit zero. Full-corpus arithmetic is `411` traversed, unexpected errors `0`,
  sentinel collisions `0`, expected negative parse errors `31`.
- DONE — L0 independently reran the schema-2 test from a Git archive: schema 1
  stayed byte-identical, schema 2 exposed `semantic.field` and aligned
  `semantic.values`, `9` annotations were traced and `27` AST nodes were
  independently checked.
- DONE — Model 1 now pins the promoted toolchain in
  `manifests/video-semantic-toolchain-pin-v1.json`: repository/tree and grammar,
  corpus, sync, retrieval and demo ancestors; `15` exact blobs; `7` executable
  probe definitions. The static pin validator is part of foundation.
- DONE — The strict schema-2 consumer preserves schema-1 compatibility, accepts
  reviewed field/value semantics, keeps enum values lazy per field and rejects
  unknown, duplicated, malformed or unpinned surfaces.

## Exact starting census

- FACT — L16 recomputed `9` video modules, `10` JSON schemas, `9` synthetic JSON
  fixtures and `155` video/census/egress tests at the baseline.
- DONE — P4A bounded Census Bridge is implemented and its dedicated roster is
  green.
- DONE — The closure roster is now `254` collected tests across the `15`
  dedicated wave modules. The only intentional skip is the environment-gated
  clean-archive execution of the already independently replayed upstream
  toolchain; its static pin validation remains mandatory in foundation.
- DONE — P2 local census, P5/P7 explicit crosswalk, P8 exact `64+32` candidate,
  P11 deterministic semantic index, P12 Brain grounding, P13 paired evaluator
  and P14 fail-closed verdict engine are implemented with adversarial tests.
- DONE — The Mac no-egress harness executes separate worker and peer processes:
  DNS/TCP attempts are denied `4/4` while the exact local peer remains usable.
- RISK — That proof uses a synthetic worker and is not evidence that the actual
  model runner has completed the same process-boundary test.
- OPEN — P4B SecretStorage integration and P6 live census belong to the VSIX
  write-set and require external capability/attestation. They cannot be
  simulated into a terminal gate.
- OPEN — P10 removal of `draft` requires a distinct authorized human editorial
  decision. Code can prepare and validate the review bundle, not impersonate
  that decision.

## Execution roster

| Phase | Current state | Closure evidence required |
|---|---|---|
| P1 | machinery complete; production ontology external | run the real no-egress worker over the authorized private bundle; validate ontology/disposition/constraints |
| P2 | builder complete; real roster external | execute the deterministic local census over the private schema-2 projection |
| P3 | complete | builders, strict schemas and negative tests remain in the default check chain |
| P4A | green | regression roster remains green |
| P4B | external | VSIX SecretStorage/process-boundary receipt |
| P5 | builder complete | materialize the preliminary exact-decision crosswalk from P1/P2 evidence |
| P6 | external | signed read-only authority + atomic live census receipt |
| P7 | engine complete; data/editorial gate external | reconcile local/live census and close critical unresolved concepts |
| P8 | candidate builder complete; terminal freeze external | ratify thresholds and seal `64+32=96`, twelve critical, leakage gaps zero before model output |
| P9 | pin and consumer complete; tenant patch external | execute canonical tenant sync only in a separately authorized worktree |
| P10 | human gate | frontier review bundle plus human promotion receipt |
| P11 | complete | deterministic revision, membership/freshness and rollback CAS tests |
| P12 | complete offline | resolve/clarify/unsupported, multi-concept, injection and catalog ambiguity tests |
| P13 | evaluator complete; model execution external | run exact B0/B1/D0/D1 observations against the terminal benchmark |
| P14 | engine complete; actual verdict `BLOCKED` until evidence exists | recompute every score from rows and emit one decision with no implicit training authority |

## Repository gate

- FACT — Baseline `make check` was `passes=80 errors=2 files=458` because the
  T30-v3 validator compared working-tree bytes with a historical preimage and
  required a vanished Node path.
- FIX — T30-v3 now reads its sealed preimage from the pinned Git object and
  selects only the exact pinned Node version. Historical artifacts remain
  byte-for-byte unchanged.
- FIX — All `27` closure artifacts and all `11` video schemas (`10` standalone
  plus the paired source-manifest schema) are mandatory foundation members.
  The promoted toolchain pin is statically validated and reserved document
  suffixes are forbidden by repository policy.
- FIX — Offline outputs are confined to immutable relative namespaces below the
  ignored private store, use no-overwrite writes and publish a bundle manifest
  last. Receipts use exact key sets and explicit redaction assertions.
- FIX — Evaluation requires exact B0/B1/D0/D1 rows bound to the terminal
  benchmark; aggregate counts, Wilson bounds, critical failures and taxonomy
  are recomputed. A caller-provided aggregate or bare boolean receipt has no
  authority.
- FIX — P14 applies critical and recurring-procedural vetoes before successor
  routing. `BLOCKED` is persisted privately and exits non-zero; no verdict can
  start training or promote an adapter.
- FACT — First integrated retry closed the historical replay:
  `FOUNDATION VALID passes=84 errors=0 files=485`; pilot contracts were also
  `VALID`. The final pytest collection then stopped before tests because the
  Makefile still forced a vanished legacy Node locator.
- FIX — The Makefile now selects the registered `v22.22.3` NVM locator instead
  of the vanished legacy path. The oracle still independently requires exact
  mode, size and SHA-256; a dedicated regression test binds the Makefile.
- FIX — The default test target now constructs a clean detached authority at
  the historical Metis oracle revision from read-only Git objects, copies and
  verifies the pinned Node toolchain, sanitizes the child environment and
  rejects any source/isolated authority mutation. Production broker STOPs stay
  real; only explicitly named capsule-interior tests receive a bounded seam.
- DONE — Oracle, W3 qualifier, bridge replay and production-adapter regressions
  are green after independent failure diagnosis. The v1/v3 blocked-report
  `native_evidence` keys are restored to their exact schema versions and the
  bridge binds the repaired qualifier digest.
- FIX — T30-v3 live semantic replay now opens one verified immutable oracle
  session for the complete base+adapter roster instead of rematerializing the
  same 219 MB / 17,312-file toolchain per source candidate. Every request still
  executes the session's post-run immutability check and session close repeats
  the terminal check; no persistent cache, hard link or skipped replay exists.
- DONE — Two regressions prove that an explicitly shared session opens no
  nested snapshot and that the complete replay uses exactly one session for
  both sides. The existing oracle mutation roster remains green (`11/11`).
- FACT — After the performance repair, the real standalone repository validator
  is `VALID`: `foundation-files=320`, `errors=0`, `repository-files=487`, total
  wall time `1:43.91`. This replaces the former roughly fifteen-minute replay
  without weakening its authority checks.
- DONE — L0 reran the promoted grammar/retrieval delivery from Git-object
  archives with its exact `v22.20.0` Node authority: evidence
  `in=15 out=15 distinct=15 gaps=0`, probes
  `in=7 out=7 distinct=7 gaps=0`. The full-corpus denominator is `411`, with
  `0` unexpected errors, `0` sentinel collisions and `31` expected negative
  parse errors. This successor authority is intentionally distinct from the
  historical Oracle gate's exact `v22.22.3` runtime; neither pin was rewritten.
- FIX — The catalog-refresh, W3 materializer and qualifier tests no longer carry
  a vanished legacy Node fallback or a false-unavailable skip. They require the
  absolute Metis/Node authorities injected by the canonical isolated harness;
  the frozen W3 runtime itself remains unchanged.
- DONE — The three affected test modules replayed through the canonical harness
  with exit `0`; the public-synthetic retrieval cases previously skipped by the
  legacy locator executed successfully.
- DONE — Final integrated `make check` is green: foundation
  `passes=84 errors=0 files=487`, pilot contracts `VALID`, lint/format clean and
  pytest `2044 passed, 2 skipped, 0 failed` in `2579.87s`; the command exited
  `0`. The two opt-in skips require, respectively, the successor grammar's
  separate runtime authority (already replayed above at `7/7`) and the external
  protected W3 production contract; neither is silently counted as executed by
  the default suite.

## Status

`AUTOMATABLE_SCOPE_COMPLETE_EXTERNAL_EVIDENCE_PENDING`
